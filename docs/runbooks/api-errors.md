# Erros da API

1. Separe os erros por rota e status no Grafana.
2. Correlacione o intervalo com traces no Zipkin e logs pelo `traceId`.
3. Procure indisponibilidade de dependencias e saturacao do pool JDBC.
4. Nao registre perguntas, respostas, tokens ou conteudo de documentos durante a investigacao.
